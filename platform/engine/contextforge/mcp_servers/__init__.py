"""MCP tool servers — graph, timeseries, vector search, compute, SQL, API, document processing.

Available servers
-----------------
- **GraphTools** — Knowledge graph entity search, relationships, and time-travel history.
- **TimeseriesTools** — TimescaleDB telemetry: latest readings, trends, parameter listing.
- **VectorTools** — Semantic search over documents and entity embeddings via Qdrant.
- **ComputeTools** — Deterministic computations: NEWS2, qSOFA, OEE, generic formulas.
- **SqlTools** — Parameterized read-only SQL queries against PostgreSQL via asyncpg.
- **ApiTools** — HTTP calls to whitelisted external APIs and FHIR R4 REST queries.
- **DocumentTools** — Text extraction (PDF/DOCX/HTML), chunking, and LLM summarization.
"""
