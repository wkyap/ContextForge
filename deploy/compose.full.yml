# ContextForge — Full profile
# Every service: data + platform + observability + auth + local LLM.
#
# Usage:
#   docker compose -f deploy/compose.full.yml up -d

name: contextforge

include:
  - path: ./services/data.yml
  - path: ./services/platform.yml
  - path: ./services/observability.yml
  - path: ./services/auth.yml
  - path: ./services/local-llm.yml
