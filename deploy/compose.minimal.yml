# ContextForge — Minimal profile
# Core platform: data stores + LiteLLM + API + Workbench.
# No observability, no Keycloak, no local LLM.
#
# Usage:
#   docker compose -f deploy/compose.minimal.yml up -d

name: contextforge

include:
  - path: ./services/data.yml
  - path: ./services/platform.yml
